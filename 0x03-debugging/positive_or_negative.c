#include "main.h"
/**
 *positive_or_negative - checking for positive or negation number
 *
 *i: The number to be checked
 *
 *Return: Always 0
 */

void positive_or_negative(int i)
{
	if (i == 0)
		printf("%d is zero\n", i);
	else
		printf("%d is not zero\n", i);
}
